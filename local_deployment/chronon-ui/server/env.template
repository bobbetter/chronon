# AWS Credentials for Remote AWS Services (S3, EMR, etc.)
# Copy this file to .env and configure your preferred authentication method.
# These use _REMOTE suffix to avoid conflicts with local AWS credentials.

# =============================================================================
# OPTION 1: AWS SSO (Recommended)
# =============================================================================
# This is the easiest method if you use AWS SSO.
#
# Steps:
# 1. Run `aws sso login --profile <your-profile>` on your local machine
# 2. Set the profile name below
# 3. The ~/.aws directory is mounted into the container (see docker-compose.yml)
#
AWS_PROFILE_REMOTE=your-sso-profile-name


# =============================================================================
# OPTION 2: Explicit Credentials
# =============================================================================
# Use these if you have static IAM credentials or temporary credentials.
# If these are set, they take precedence over the profile.
#
# AWS_ACCESS_KEY_ID_REMOTE=your-access-key-id
# AWS_SECRET_ACCESS_KEY_REMOTE=your-secret-access-key
# AWS_SESSION_TOKEN_REMOTE=your-session-token  # Optional, for temporary credentials

