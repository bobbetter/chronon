- Run `spark-shell -i ./scripts/data-loader.scala --master local[*]` to load data into spark and create tables.

- Run `uv pip install -e /Users/dhamerla/Apps/chronon-zipline/python` to install the the full chronon-zipline python package,
which (unfortunately) is different than what is available on pypi.