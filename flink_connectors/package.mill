package build.flink_connectors
import mill._

// Namespace-only parent. Each connector lives in its own sub-module (pubsub/, kinesis/, ...)
// with isolated dependencies.
object `package` extends Module
