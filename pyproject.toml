[project]
name = "chronon-zipline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.4.1",
    "fastapi[standard]>=0.119.0",
    "pandas>=2.3.3",
    "pytest>=8.4.2",
]
